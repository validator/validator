matrix:
  platform:
  - debian11
  - ubuntu2004
  - macos
  - macos_arm64
  - windows
  bazel:
  - 7.x
  - 8.x
  - 9.x
tasks:
  verify:
    name: Verify vnu jar
    platform: ${{ platform }}
    bazel: ${{ bazel }}
    build_targets:
    - '@vnu//:vnu.jar'
